apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: status-board
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: status-board
                port:
                  number: 80